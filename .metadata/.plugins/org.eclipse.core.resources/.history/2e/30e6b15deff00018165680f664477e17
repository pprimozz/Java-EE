package com.telusko.web;

import java.io.IOException;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.telusko.web.dao.AlienDao;

/**
 * Servlet implementation class CreateAlienController
 */
public class CreateAlienController extends HttpServlet {
	
	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		
		int aid= Integer.parseInt(request.getParameter("aid"));
		int aname= Integer.parseInt(request.getParameter("aname"));
		int tech= Integer.parseInt(request.getParameter("tech"));
		
		
		
		AlienDao ad = new AlienDao();
		//ad.createAlien(aid, aname, tech);
		
	}

}
